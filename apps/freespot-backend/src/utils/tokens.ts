// signAccessToken(payload): string

// verifyAccessToken(token): payload

// generateRefreshToken(): { token: string; jti: string }

// hashRefreshToken(token: string): string // HMAC with REFRESH_HMAC_SECRET
// JWT_PRIVATE_KEY

// JWT_PUBLIC_KEY

// REFRESH_HMAC_SECRET
