{
  "version": 2,
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "commands": {
      "build": "npx nx build FreeSpot"
    },
    "outputDirectory": "dist/apps/FreeSpot"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
