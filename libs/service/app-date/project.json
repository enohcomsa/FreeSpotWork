{
  "name": "free-spot-app-date",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/service/app-date/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:service"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/service/app-date/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/service/app-date/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "libs/service/app-date/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/service/app-date/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
