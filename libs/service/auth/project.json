{
  "name": "free-spot-auth-service",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/service/auth/src",
  "prefix": "free-spot",
  "projectType": "library",
  "tags": ["scope:service"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/service/auth/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/service/auth/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "libs/service/auth/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/service/auth/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
