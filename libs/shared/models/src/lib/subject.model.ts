/**
 * @deprecated Legacy Firebase-era interface.
 * Replaced by the new Subject domain model (e.g. `SubjectResponse`, `SubjectBase`, etc.)
 * from the FreeSpot API. Avoid using this type in new code.
 */
export interface SubjectItemLegacy {
  name: string;
  shortName: string;
}
